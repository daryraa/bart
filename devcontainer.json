{
    "name": "FastAPI Dev Container",
    "postCreateCommand": "pip install -r requirements.txt && python -m uvicorn main:app --host 0.0.0.0 --port 8000 --reload > uvicorn_log.txt 2>&1",
    "customizations": {
        "vscode": {
            "settings": {},
            "extensions": [
                "ms-python.python"
            ]
        }
    },
    "forwardPorts": [8000],
    "remoteUser": "vscode"
}
